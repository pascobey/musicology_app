<div class="d-flex flex-column">
    <div class="d-flex flex-row">
        <div class="d-flex flex-column m-1 border border-dark align-self-center rounded" style="width:70vw;">
            <p class="h2 mb-0 px-2">Your Music</p>
            <div class="d-flex flex-row m-1 border border-dark rounded justify-content-start">
                <div id="user-music-container" class="d-flex flex-column m-2">
                    <div class="d-flex flex-row border border-light border-top-0 border-right-0 border-left-0 w-100">
                        <a id="playlists-btn" class="h6 mb-0" href="#">Playlists</a>
                    </div>
                    <div class="d-flex flex-row border border-light border-top-0 border-right-0 border-left-0 w-100">
                        <a id="artists-btn" class="h6 mb-0" href="#">Artists</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="d-flex flex-column">
    <div class="d-flex flex-row">
        <div id="playlists-container" class="d-none flex-column m-1 border border-dark align-self-center rounded" style="width:70vw;">
            <p class="h2 mb-0 px-2">Your Playlists</p>
            <div class="d-flex flex-row m-1 border border-dark rounded justify-content-start">
                <div id="user-playlists-container" class="d-flex flex-column m-2">
                    <% @user.library.playlists.each do |p| %>
                        <div class="d-flex flex-row border border-light border-top-0 border-right-0 border-left-0 w-100">
                            <%= "'#{p.name}'" %>
                        </div>
                    <% end %>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="d-flex flex-column">
    <div class="d-flex flex-row">
        <div id="artists-container" class="d-none flex-column m-1 border border-dark align-self-center rounded" style="width:70vw;">
            <p class="h2 mb-0 px-2">Your Artists</p>
            <div class="d-flex flex-row m-1 border border-dark rounded justify-content-start">
                <div id="user-playlists-container" class="d-flex flex-column m-2">
                    <% Artist.where(library_id: @user.library.id).each do |a| %>
                        <div class="d-flex flex-row border border-light border-top-0 border-right-0 border-left-0 w-100">
                            <%= "#{a.name}" %>
                        </div>
                    <% end %>
                </div>
            </div>
        </div>
    </div>
</div>