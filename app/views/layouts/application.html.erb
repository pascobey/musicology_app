<!DOCTYPE html>
<html lang="en">
  <head>
    <title>bumusicology | Link to Spotify</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body style="background-color:rgb(248,248,255);">
    <!-- notice bar - flex-row -->
    <div id="flash" class="d-flex flex-row justify-content-center shadow w-100" style="background-color:rgb(255,240,245);">
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, class: "#{name}" %>
      <% end %>
    </div><!-- notice bar - flex-row -->
    <!-- Content Containter - flex-row -->
    <div id="page-container" class="justify-content-center">
      <!-- Navbar container -->
      <nav id="navbar" class="navbar">
        <%= link_to(image_tag("main.png", width: "40", height: "40"), "/", class: "navbar-brand") %>
      </nav><!-- Navbar container -->
        <!-- yield container -->
        <div id="yield-container" class="d-flex flex-row">
          <%= yield %>
        </div><!-- yield container -->
    </div><!-- Content Containter - flex-row -->
  </body>
</html>
